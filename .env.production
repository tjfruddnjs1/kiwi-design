# ====================================
# Production Environment Configuration
# ====================================
#
# Kubernetes 배포 환경 설정
# - k8scontrol: k8s-control 네임스페이스
# - AST Service: ast---line-world-service 네임스페이스
#
# Backend 프록시를 통한 간접 호출 방식 사용
# Frontend → Backend (k8s-control) → AST Service (ast---line-world-service)
# ====================================

# Backend API Proxy Target (Kubernetes Service)
VITE_API_PROXY_TARGET=http://k8scontrol-backend:8080

# ====================================
# AST Service Configuration (Backend 프록시 방식)
# ====================================
# AST 서비스는 Backend 프록시를 통해 호출됨
# Frontend는 /api/v1 경로로 호출하면 Backend가 자동으로 AST 서비스로 전달
#
# Backend AST_API_URL (K8s): http://api-gateway.ast---line-world-service.svc.cluster.local:7000
# Frontend → /api/v1/sbom/image → Backend → AST /scan/sbom/image
# ====================================

# AST 서비스 Base URL (Backend 프록시 경로)
VITE_AST_SERVICE_URL=/api/v1

# ====================================
# Scanner Endpoints (Backend 프록시 사용)
# ====================================
# 주의: 이 설정은 analysisApi 직접 호출 시에만 사용됨
# 권장: gitApi를 통한 Backend 프록시 호출 (이미 구현됨)
# ====================================

# Trivy Container Image Scanner (사용 안 함 - gitApi.trivyScanImage 사용)
# VITE_TRIVY_ENDPOINT는 설정하지 않음 → 기본값 사용: /api/v1/scan/trivy/scan/image

# ZAP DAST Scanner (사용 안 함 - gitApi.dastScanWeb 사용)
# VITE_ZAP_ENDPOINT는 설정하지 않음 → 기본값 사용: /api/v1/scan/zap/scan

# SAST Scanner (Backend /gits action을 통해 호출)
# VITE_SAST_ENDPOINT는 설정하지 않음

# ====================================
# Environment Settings
# ====================================

# Disable verbose proxy logging in production
VITE_PROXY_VERBOSE_LOGGING=false

# Environment Identifier
VITE_APP_ENV=production
